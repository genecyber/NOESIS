#!/bin/sh

# Bidirectional memory sync before commit
# Pulls from remote, merges with local, pushes merged set back
echo "🔄 Syncing memories (bidirectional)..."
npx tsx scripts/sync-memories.ts

# Continue with commit even if sync fails (network issues shouldn't block commits)
exit 0
